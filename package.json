{
  "name": "blu_parser",
  "version": "1.0.3",
  "description": "blu_parser",
  "license": "ISC",
  "author": "Blu",
  "type": "commonjs",
  "main": "index",
  "scripts": {
    "test": "npm run build && node ./lib/test",
    "build": "tsc",
    "benchmark": "npm run build && node ./lib/benchmark"
  },
  "files": [
    "lib/**/*"
  ]
}
